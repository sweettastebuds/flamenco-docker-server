services:
  manager:
    build:
      context: .
      args:
        FLAMENCO_VERSION: "3.8.2"
        BLENDER_VERSION: "5.0.1"
    image: flamenco:latest
    container_name: flamenco-manager
    restart: unless-stopped
    volumes:
      - ./build/flamenco-manager.yaml:/home/flamenco/flamenco-manager.yaml
    ports:
      - "780:8080"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/api/v3/version"]
      interval: 30s
      timeout: 10s
      start_period: 15s
      retries: 3
